# ── "Ask about Sandipan" chat — optional LLM upgrade ──
# The chat works WITHOUT any of this (the built-in engine answers for free).
# Copy this file to `.env.local` (git-ignored) to enable an LLM. Pick ONE path:


# Free token from https://huggingface.co/settings/tokens (role: Read).
# ⚠️ A VITE_ token is bundled into the public JS — do NOT use this on the live site.
VITE_HF_TOKEN=hf_your_token_here
